---
id: ADR-0009
type: adr
status: accepted
date: 2026-09-16
author: heiniao
created: 2026-09-16
updated: 2026-09-16
aliases: [ADR-0009]
provenance: 用户问"删除/转移时 id 要不要变？id 应该是快照、不可修改？"—— 这是基座问题，不是实现问题
---

# ADR-0009 id 是不可变快照

## 背景

约定层收敛（19 → 8）后，`agreements/` 里留下缺口：`A1 A2 A5 A7 A8 A9 A11 A15 A17 A18 A19` 都不在了。

由此产生三个必须回答的问题：

1. 删除/转移条目时，**id 要不要跟着变**（重编号，保持连续）？
2. 整体目录下的文件**要不要一起改**？
3. 编号会越来越大（`S100+`），不利于使用 —— 那能不能重排？

## 决策

**id 是不可变快照。永远不重编号。**

| 问题 | 答案 | 理由 |
| :--- | :--- | :--- |
| 删除后要重编号吗 | **不要** | 缺口就是**死亡记录**。补上缺口等于抹掉"这里曾经死过一条" |
| 转移时要改 id 吗 | **不要** | id 是身份，路径是住所 —— 搬家不改身份证号 |
| 目录下其他文件要改吗 | **不要** | 重编号是**最贵的错误**：ADR、evolution-log、`git log`、外部引用会一起失效 |
| 编号太大怎么办 | **靠语义后缀，不靠重排** | Git 用 40 位哈希、Jira 用 `PROJ-1234` —— 没人因为编号长就重排 |

### 身份与描述的分离

```
S12-边界解析
└─┬─┘ └──┬──┘
  │      └─ 描述：可改（改语义、改措辞）
  └──────── 身份：不可变
```

**编号是身份，语义后缀是描述。** 引用以**编号前缀**为锚，才对得起"id 不可变"。

## 后果

正面：

- 任何历史记录（ADR / 日志 / 对话）**永远引用得到**。
- 改名（语义后缀）不影响身份。

负面：

- `agreements/` 的编号会长期是 `A3 A4 A6 A10 A12 A13 A14 A16` —— **看起来不连续**，
  新读者可能误以为"文件丢了"。需要在 `_index.md` 说明**缺口是刻意的**。

## 替代方案

- **删除后重编号保持连续** —— 否决：见上，代价是全库引用 + 全部历史失效。
- **改用纯语义 id（`boundary-parsing`）** —— 否决：丢掉"口头引用短"和"天然有序"两个好处；
  且纯语义 id 在措辞变化时要整体改名。
- **给编号加前缀字母池扩容（`SA1`）** —— 暂不必要：`S100+` 并不影响使用，只是不好看。

## 未决（需要用户拍板）

**链接引用应该锚在"编号前缀"还是"完整文件名"？**

（下文的链接示例改用**全角方括号**书写 —— 因为 `collab validate` 目前无法区分
"真链接"与"正文里提到的链接形状"，用半角方括号会被误判成死链。）

- 锚**编号前缀**（形如 `［S12］` 的双括号）：语义后缀改名后**链接存活** —— 与"id 不可变"一致。
- 锚**完整文件名**（形如 `［S12-边界解析］` 的双括号）：更可读，但**改名即断链**。

2026-09-16 的规则要求"用文件名、不用 id"（见 `meta/naming-conventions.md`）。
本条决策（id 不可变）**与该规则存在张力**，需要一并确认后再改解析器。

## 关联

[[meta/naming-conventions]] [[meta/pruning-policy]] [[A6-version-authority]] [[ADR-0008-约定层收敛完成]]
