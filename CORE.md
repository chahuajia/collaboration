---
id: CORE
type: meta
status: active
created: 2026-09-25
updated: 2026-09-25
author: heiniao
provenance: 09-16 → 09-25 三个仓（collaboration / collab-cli / evolutionary）的实测记录
aliases: [CORE]
---

# 核心：这套系统里最值钱的技术与思想

> ⏱ **所有数字截至 2026-09-26**。它们**会腐烂** —— 要当前值，跑命令：
> `collab validate`（全部条目）· `collab catalog`（路由中条目）·
> `grep -c "^| 2026-" meta/interceptions.md`（拦截数）。
> 本文件曾在同一天里三处数字互不一致（126 / 121 / 129），见文末更正记录。


> `SHARE.md` 是对外分享稿（叙事 + 判据 + 代价）。**本文件是技术内核**：
> 把 126 个条目压成「机制 / 判据 / 不变式 / 继承蓝图 / 诚实边界」。
> 读完这一篇，你可以不读那 129 条就开始搬。

## 一句话

值钱的不是 129 条正文，是 **12 个机制 + 11 条判据 + 4 个不变式**，
以及**证明它们成立的证据链**（哪些有硬证据、哪些至今不可判定）。

---

## 一、机制清单（技术）

机制 = **有接线点的东西**。没有接线点的，不在此列 —— 那是政策（见判据 8）。

| # | 机制 | 接在哪 | 证据 |
| :--- | :--- | :--- | :--- |
| M1 | `collab validate` 的九类校验 | CI（`.github/workflows/validate.yml`）+ 本地 | 206 issues → 0；`UNDECLARED_DIR` 把"加目录"变成改基座 |
| M2 | **两本账本**（收益 / 缺口） | W4 收口仪式 | 16 条真实拦截 + 0 条开放缺口；账本成对才有方向 |
| M3 | **入库门槛 `falsifier`** | `collab new` / validate | 2026-09-19 起：入库必须答"不读它会照本地哪个模式写错" |
| M4 | **毕业 `enforced`** | validate + 路由面检查 | 3 条已毕业（内容进了测试 → 退出索引） |
| M5 | **退役 `retire`** | `collab retire` | 标记-清除（种子=根文档+无向）；已退役 1 条 |
| M6 | **路由三层**：症状表 → catalog → 报告缺口 | `AGENTS.md` + `catalog.json` | 实测命中率：症状表 2/4 > catalog 0/4（故症状表优先） |
| M7 | **基座契约 + 破坏性变更三件套** | `meta/base-contract.md` | 09-16 一天改四次基座、零迁移脚本 → 才立的这条 |
| M8 | **生成物防陈旧** | `collab index` / `collab catalog` | `CATALOG_STALE` / `MISSING_FROM_INDEX` |
| M9 | **多仓分工 + 证据分层** | 三仓布局 + `project-evidence-vs-kb-ledger` | 证据全文留业务仓，KB 只留一行摘要 → 否则 KB 变成项目日记 |
| M10 | **派工契约**（worktree 重新基线 / 回执 / 派出会计 / 成本门槛） | `extreme-unattended-cluster` + S36 | 同树四写、找不到刚写的文件、小切片负吞吐 —— 全部实测踩过 |
| M11 | **工具面即约束** | 工具表本身 | "AI 不 commit"写进散文被无视；写进工具表则**不可能违反** |
| M12 | **仪器 > 政策** | 三问 + 接线点 | 接线的当天抓出 2/3 仓已烂；没接线的空转两天 |

### 三个最不容易被想到的

**M4 毕业** —— 最值钱的一条规范（"领域层不许碰框架"）被写成架构测试后，
**条目本身退出索引**。它没过时，它*完成*了。
「一个会收缩的知识库」这个想法，比"有 129 条"重要得多。

**M11 工具面即约束** —— 规则的强度取决于它接在哪：

| 同一个规则 | 接在哪 | 结果 |
| :--- | :--- | :--- |
| 不许 AI commit | 散文 | 被无视 |
| 不许 AI commit | **工具表里根本没有 commit 这个工具** | **物理上不可能违反** |

**M9 证据分层** —— KB 里只留一行摘要，证据留业务仓。
这一条决定了 KB 能不能长期活着：不留它，KB 会被项目日记淹死（实践里已经淹过一次）。

---

## 二、判据清单（思想）

每条都带**反例或证据** —— 没有证据的判据不进这张表。

| # | 判据 | 证据 / 反例 |
| :--- | :--- | :--- |
| J1 | **生产变便宜 → 价值迁移到选择与验证** | A10 的推广：实现免费→review 上移；条目免费→修剪上移 |
| J2 | **最危险的不是无知，是恭顺** | 16 条拦截里 **5 条原文用的是"继续"**（差点继续 `startsWith` 判状态码…），没有一条是"模型不知道" |
| J3 | **机制只管它被安装的地方** | n=3 自然实验：被新颖度脚本盯的仓新鲜；没盯的一个落后 21 提交、一个根本没登记。100% 相关 |
| J4 | **活动 ≠ 产出；机械应用撞不到墙** | 111 波 L2，前端测试 ×5.3，**L3 增量 = 0** |
| J5 | **核实的范围由你的想象决定** | 同动作两次不同遗漏：一次没核实数据源存在，一次核实了生产却漏了 3 个测试替身 |
| J6 | **声称需要被强制，否则静默漂移** | 注释写"同事务双写"，全仓**零 `@Transactional`** → 数据错且静默 |
| J7 | **让知识能死** | 毕业（3 条）/ 退役（2 条）/ 缺口（0 条）；反之 79KB bundle 是只增不减的产物 |
| J8 | **无机制的政策是负优化** | v5→v9 一路加条文，吞吐反而变慢；三问（可观察/强制动作/执行面）任一否 → 别写 |
| J9 | **给停滞装仪器，测"产出为 0"，不要测"积压 > 0"** | 候选池龄期仪器**永远恒绿** —— 因为候选根本不产生。**绿线仪器比没仪器更危险** |
| J10 | **只有收益侧、没有缺口侧的账本会变成功劳簿** | 所以 `interceptions` 与 `known-gaps` 必须成对 |
| J11 | **红线也要配动作：只有"更新"和"降级归档"两个合法响应** | 2026-09-25 实测 `collab memory`：**6/7 个 WM 文件过期**，红灯亮着，无人接。这是 J9 之后漏掉的第四态 |

### 两条最锋利的

**J2 恭顺 vs 无知** —— 它决定一条规范会不会随模型升级自动贬值：

- 拦**无知** → 模型变强就废纸；
- 拦**恭顺**（照着项目里的旧代码写下去）→ **永不贬值**，因为旧代码永远在。

⇒ 所以入库问法不是"AI 知不知道"，而是 **"不读它，模型会照着本地哪个模式做错？"**（M3）

**J9 仪器四态** —— 一层比一层贵，第四态是 2026-09-25 才补齐的：

```
① 没仪器                      → 从未执行（引用计数 / 时间修剪 / 代谢配额）
② 有仪器没接线                 → 也未执行（check-freshness 空转两天）
③ 有仪器、接线、但测错了病       → **恒绿** —— 比没装更危险，因为它看起来像在监控
④ 有仪器、接线、测对了、**红了**  → **仍不执行** —— 因为没人规定"红了怎么办"
⑤ 有仪器、接线、红了、**且有强制动作** → 真正生效（接线当天抓出 2/3 仓烂）
```

**第 ④ 态是 2026-09-25 新补的**：`collab memory` 当场报出
`working-memory: 6 of 7 file(s) need attention`（8–9 天未更新），
红灯亮着，而唯一响应是"知道了一件事"。

⇒ 所以装仪器时要**同时写响应协议**：红灯出现后，只允许两个动作 ——
**更新它**，或**降级成归档**（`_archive/` 快照不受检查）。
「知道了但不动」不是第三个选项，它是这条仪器失效的定义。

---

## 三、四个不变式

这三仓九天的所有改动，可以归到四条不变式上。**动它们 = 破坏性变更。**

| 不变式 | 一句话 | 强制方式 |
| :--- | :--- | :--- |
| **基座冻结** | 目录 / kind / id / frontmatter / 链接 / 生成物不可随手动 | ADR + 迁移脚本 + validate 归零（三样缺一不可） |
| **单一真相源** | 文档指向代码；证据留业务仓；KB 只留摘要 | `UNDECLARED_DIR` / 账本落点表 |
| **仪器优于政策** | 答不出"接在哪根线上"，就不入库 | `falsifier` + 三问 |
| **知识必须能死** | 毕业 / 退役 / 缺口，三条出口都要通 | `enforced` / `retire` / `known-gaps` |

> 基座冻结那条的由来最直接：**2026-09-16 一天之内改了四次基座，四次都没有迁移脚本**。
> 在"一个人 + 一个 agent"的规模下侥幸没出事 —— 但那是运气，不是设计。

---

## 四、继承蓝图：把 129 条压成 5 个文件

如果你想在**别的项目**上试这套东西，不要搬 126 条正文。搬这五样：

```
① AGENTS.md              薄入口 + 症状路由表（第一路由）
② meta/interceptions.md  收益账本：这条拦住了什么
③ meta/known-gaps.md     缺口账本：这条没能回答什么
④ meta/pruning-policy.md 修剪：标记-清除 / 退役 / 毕业
⑤ collab validate        门禁，且必须接进 CI 或 hook
```

### 接入顺序（顺序不能换）

1. **先装门禁**（M1）—— validate 能跑、能红。没有它，后面全是散文。
2. **再立两本账本**（M2）—— 空表就行，格式比内容重要。
3. **加入库门槛**（M3）—— 从第一条新条目起就要求 `falsifier`。
4. **写薄入口 + 症状表**（M6）—— 按"我要干什么"组织，**不要按条目清单组织**。
5. **给仪器接线**（M12）—— 每条规则问：它接在哪？接不上的先别写。
6. **开三条出口**（M4/M5）—— 毕业 / 退役 / 缺口，缺一条就会只增不减。
7. **最后才写正文** —— 而且只写"不读它，模型会照着本地哪个模式写错"的那些。

### 移植时最容易搞错的三件事

- ❌ **把 KB 当项目日记** → KB 会淹死。证据留业务仓，只回填一行（M9）。
- ❌ **写了规则没写接线点** → 那是负优化（J8）。先找接线点，再写规则。
- ❌ **给"积压"装仪器** → 会恒绿（J9）。测产出是否为零。

---

## 五、诚实边界（哪些结论站得住，哪些不站）

这一节是本文件与"营销稿"的分界线。

| 结论 | 证据等级 | 说明 |
| :--- | :--- | :--- |
| 机制（测试 / CI / hook / 工具面）能改变行为 | **硬** | n=3 自然实验；接线当天抓出 2/3 仓已烂 |
| 条目内容能改善产出 | **弱** | 唯一干净的对照实验（空项目双臂）测出**零差异**，读库臂墙钟 **+55%** |
| **"读了有没有用"** | **至今不可判定** | 13 条账本全部是自述；没有反事实臂 |
| 我删过自己的假绿灯 / 改过与事实相反的规则 | 硬 | `echo TODO` 的 CI 已换成真 validate；"AI 永远不持有写权限"已改 |

### 三条必须一起读的更正记录

1. **A16 的技术前提曾写错**（"上下文先进先出、机械截断"）—— 现代客户端是压缩而非 FIFO。
2. **`patterns/review-marginal-value` 里"边际价值增加 3-5 倍"是编造的数字** —— 无来源，已删。
3. **（已删）`WRITEUP.md` 曾写"真实拦截记录 14 行"** —— 当时实际是 **13 条真实 + 1 行样例**。
   该文件已并入 `SHARE.md`：同一内容写三处（ARTICLE / WRITEUP / SHARE），
   三处数字各自腐烂过 —— **例子本身成了它讲的那条判据的证据**。
   这正是它自己那条"手写的可计算量必然腐烂"的又一例：**数行数请用命令，不要手写。**

> 这三条留在这里不是自罚 —— 是**证明这套系统会抓住自己的错**。
> 一个不会否定自己的知识库，和一份营销稿没有区别。

---

## 六、它会怎么死

一份只说优点的技术文档是不诚实的。这套系统有四个已知死法，**每一个都有先例**：

| 死法 | 先例 | 现有的刹车 | 刹车接上了吗 |
| :--- | :--- | :--- | :--- |
| **变成散文工厂**：只加不减，越写越像"都对但没用" | 79KB bundle | 两本账本 + 修剪政策 | 部分（账本在跑） |
| **跑得很欢、什么都没产出** | 111 波 L2 / L3 = 0 | pressure-routing 写着"连续 5 tick 无增量 → 停" | ❌ **判据存在，但没有仪器去数它** |
| **仪器恒绿，被无视** | 候选池龄期检查 | "测产出为 0，不测积压 > 0" | ⚠️ 判据已立，仪器未装 |
| **仪器亮了红灯，没人接** | 2026-09-25：6/7 个 WM 文件过期 | 无（缺响应协议） | ❌ **红灯已亮 8–9 天** |
| **退回遗传漂变**：种群为 1，无外部选择压力 | 系统早期阶段 | 引入第二个真实项目（evolutionary） | ✅ 已引入 |

**第一优先级的缺口，就是第二行**：它已经有判据、有先例、成本只要一条命令 ——
而它至今没有接线。**这套系统最该修的地方，它自己已经写下来了。**

---

## 关联

[[SHARE]] 对外分享稿 · [[ROOT]] 结构 · [[meta/base-contract]] 基座 ·
[[meta/interceptions]] 收益账本 · [[meta/known-gaps]] 缺口账本 · [[meta/pruning-policy]] 修剪 ·
[[patterns/policy-without-mechanism]] · [[patterns/project-evidence-vs-kb-ledger]] ·
[[patterns/self-bootstrapping-requires-fixed-core]] · [[patterns/tests-encode-assumptions]]
