# 生产变便宜之后，什么变贵了

> 八天，四个仓，一个知识库。
> 最后发现最值钱的不是里面的知识，是**它抓住自己错误的那套办法**。

---

## 一、起点

AI 让"生产"变便宜了：写代码、写文档、写条目、开一个新 agent，边际成本都趋近于零。

于是瓶颈换了位置。它不在生产，在**选择**：

- 生成一个条目很便宜 → 条目无限膨胀 → 信噪比归零
- 开一个 agent 很便宜 → 并行度无限膨胀 → 协调成本吃掉全部收益

**当变异免费，全部价值迁移到"选择"和"验证"。**

下面这些，是八天里为这句话付出的代价换来的。不是十一条并列的道理 —— 它们大多是从**同一个错误**里长出来的。

---

## 二、机制只管它被安装的地方

三个仓库里，只有一个被一个新鲜度脚本盯着。

跑一次：**被盯的那个是新鲜的；没被盯的两个，一个落后 21 个提交，一个根本没登记。**

100% 相关。

这条不是"知识库有用"的证据 —— 它是更朴素的一件事：

> **规则有没有用，不取决于它写得多好，取决于它接在哪里。**

同一条规律在这八天里反复出现：

| 我们写了 | 它接在哪 | 结果 |
| :--- | :--- | :--- |
| 一个新鲜度脚本 | **没接线**，空转两天 | 无 |
| 同上，接进 git hook | 每次 push | **当天抓出 2/3 仓已烂** |
| "禁止 AI commit" 写成文档 | 散文 | 被无视 |
| 同一个规则写进**工具表**（压根没有 commit 这个工具） | 结构 | **不可能违反** |

最后一行是这条思想的极端形式：**最有效的规则，是让违反它在物理上不可能。**

---

## 三、真正危险的不是无知，是恭顺

我们曾经以为，规范的价值是"告诉 AI 它不知道的事"。

账本上八条拦截记录，逐条读原文，会发现一件怪事 —— **五条用的词是"继续"**：

- 差点**继续**用字符串前缀判断 HTTP 状态码
- 差点**继续**把首页写成客户端渲染
- 差点**继续**把数据库注解标在领域对象上

**没有一条是"模型不知道"。** 每一条都是"模型知道，但它照着本地已有的坏代码写下去了"。

> 模型最危险的不是无知，是**恭顺**。

这个区分决定了一件事的生死：

- 如果规范拦的是**无知** —— 那它会随模型升级自动贬值，最后变成废纸
- 如果规范拦的是**恭顺** —— 那它**永远不会自动贬值**，因为项目里永远有旧代码

所以判断一条规范该不该留，问法不该是"AI 知不知道这个"，而是：

> **不读它，模型会照着本地哪个模式做错？**

答不出来，就别写。

---

## 四、静默的失效，比吵闹的失效危险得多

### 4.1 "同事务双写"

某个用例的注释上写着这个词。听起来很可靠。

而整个仓库**没有一处 `@Transactional`**。

后果不是慢，是**数据错**：两次写各写各的，第二次失败时第一次已经提交 ——
货被取走了，账上没有记录。**而且用户那侧看起来一切正常。**

修它的方式不是改注释，是**先写一个会红的测试**证明它现在不成立，再把机制补上。

> 代码里有一类注释，不描述"这段在做什么"，而是**声称一个跨切面属性**
> —— 同事务、线程安全、已校验。
>
> 读到这种，先花五秒 `grep` 一句：**谁在强制它？**

### 4.2 台"恒绿"的仪器

同一个道理，我后来在一个更隐蔽的地方又犯了一次。

我们发现有一类文件"有入口没出口"—— 记录了待办，但流程的下一步**没有任何东西触发**。于是我给它们装了台仪器：**挂太久就报警**。

装完跑一遍，绿的。我很满意。

一天后核对：**那类文件里最新的记录，是两天前的。中间一百多个波次，新增为零。**

我假设的病是"**东西堆积、没人处理**"。
实际的病是"**东西根本不产生**"。

**所以那台仪器永远不会亮。它测的是一个不存在的病。**

> 没接线的仪器等于没装。
> 但还有更坏的一档：**接了线、却恒绿的仪器**。
> 它比没有更危险，因为**它看起来像在监控**。

由此得到一条判据，我现在的每个"健康检查"都拿它过一遍：

> **给"停滞"装仪器，要测「产出为 0」，不要测「积压 > 0」。**
>
> 积压只是停滞的一种形态。"什么都不产生"是另一种 ——
> 更常见，更容易漏掉，**因为它看起来像在正常工作**。

---

## 五、加规则反而更慢，那不是"还没优化够"

有一段时间，同一个流程从 v5 写到 v9。每版都在加条文、加门禁、加"不要…"。

吞吐**没有变快，变慢了**。

我们给这个现象起了个名字，因为它值得有个名字：**无机制的政策是负优化**。

判定只要三问（任一答"否"，就停下，先装机制）：

1. 这条规则的**违反**，能在 60 秒内被外部观察到吗？
2. 发现之后，有**强制动作**吗？（不是"记一笔"）
3. 执行面具备吗？（工具、脚本、约束）

三问都不合格的规则，写下来的唯一效果是**增加协调成本**。

这条后来反过来咬了我们自己一口。政策里早写着一条刹车：

> "连续 5 个周期没有新知识 → 停止。"

实际连续 **111 个周期**为零，没人停。

**判据存在、可观察、但没有任何东西去数它。** 又一个三问全否。

---

## 六、换一条线继续跑，不等于换了压力

有一天我离开，第二天回来看那个"用业务项目压测知识库"的任务：

| | 离开时 | 回来时 |
| :--- | :--- | :--- |
| 波次 | 58 | **169** |
| 测试数 | 35 | **185** |
| **新增知识** | — | **0** |

去看到底在做什么。答：**把已经成熟的写法，一个域一个域地铺过去。**

**机械应用撞不到墙。撞不到墙，就产不出新知识。**

我们先把一条线（后端持久化）挖到矿脉尽头，然后换了条线（前端视图模型），
又挖到尽头 —— 但我们以为**换了线就是换了矿**。

这条和上一条是同一个病的两面：

> **活动 ≠ 产出。** 跑得多，不等于走得远。
> 而这两件事在仪表盘上长得一模一样。

---

## 七、核实的范围，由你的想象决定

同一个动作，我连续两次犯了不同形态的错：

- 第一次：下令"改成服务端渲染"，**动手时才发现后端根本没有读接口**
- 第二次：核实了生产代码，**却漏了三个测试替身**，编译当场失败

**两次都不是"没核实"，是"核实的范围不够"。**

> 动手前，列出这条改动会碰到的**所有实现者**。
> 列不全的理由从来不是懒，是**没想到有这一类**。

这条有个更一般的说法，它解释了为什么"写测试"常常给出**虚假的信心**：

> 测试里出现的形态，是**作者脑子里**的形态。
> 而作者脑子里的形态，正是他误解的那部分。
>
> 所以测试通过，不是因为东西对，是**因为它和你的误解一致**。

---

## 八、让知识能死

一个知识库最容易死在自己手里：只增不减。

我们试过一条相反的规矩：**条目可以"毕业"。**

最值钱的那条规范 —— "领域层不许碰框架" —— 后来被写成了一个架构测试。从那一天起，
任何一次构建都会检查它。

**于是那条规范本身退出了索引。**

它没有过时，它**完成了**。内容活在测试里，不再需要任何人去读。

> 一个会**收缩**的知识库，比一个"有 121 条"的知识库可靠得多。

配套的还有一条入库门槛，它筛掉了绝大部分"看起来很有道理"的话：

> **新增一条，必须能说出它拦住了什么。说不出来，就别进库。**

以及两个必须成对的账本：

| 记什么 | 决定什么 |
| :--- | :--- |
| 这条规范**拦住了**什么 | 该留哪条 |
| 这条规范**没能回答**什么 | 该补哪条 |

**只有收益侧、没有缺口侧的账本，会变成一份功劳簿。**

---

## 九、最后

回到开头：生产变便宜之后，什么变贵了？

**"判断"变贵了。** 判断一条知识该不该进来，判断一条规则有没有生效，
判断一个系统是不是在真正前进。

而判断有个特点：**它不能靠加东西解决。** 加条目、加规则、加 agent、加门禁 ——
这些都在增加生产，而生产已经便宜到没有价值。

八天里最有用的事，几乎都是**减**：

- 删掉一个假绿灯的检查（那个脚本 `echo` 一行就当作通过，五项校验一项没做）
- 删掉一个编造的数字（"边际价值增加 3-5 倍"，没有任何来源）
- 改掉一条与事实相反的规则（"AI 永远不持有写权限" —— 它有）
- 写一条规范，专门用来**指控自己最近三个版本的负优化**

**这套东西最像样的能力，是它否定自己的能力。**

它已经知道自己该砍什么、该停什么、该接哪根线了。
答案都写在它自己的文本里，只是还没执行回自己身上。

而这恰好是它想说的最后一件事：

> **一个知识系统的价值，不在于它知道多少，
> 而在于它能不能在没有外部监督的情况下，抓住自己的错误。**

---

*一整套东西 —— 包括上面每一个失败 —— 都在仓库里。*
