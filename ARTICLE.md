# 一个知识库如何学会否定自己

> 八天、121 个条目、293 个提交之后，我发现这套东西最值钱的不是任何一个条目。
>
> —— 一次关于"AI 让生产变便宜之后，人该记录什么"的实验记录。

## 一、先给一个不体面的结论

我们做过一次对照实验，想验证"让 AI 读规范有没有用"。

装置：同一个任务（用 DDD 写一个换电域），**两臂**。A 臂不读任何规范；B 臂按正规路径读完知识库再做。

结论：**关键指标完全相同。代码结构一模一样。**

而且 B 臂**更慢** —— 多花的时间几乎全在读库上。

如果这是个产品发布会，故事到这儿就该结束了。但真正有意思的是**接下来发生的事**：我们把这条结论**写进了知识库本身**，作为那个知识库的价值依据之一。

八天后回看，那次实验教给我们的东西，比它否定的东西多得多。

## 二、它测错了什么

那次实验有个致命的边界，我们当时没看出来。

它测的是：**模型知不知道正确答案。**

而它真正该测的是：**模型会不会被眼前的代码带偏。**

证据在别处。我们后来维护了一个"拦截账本"，记录"某条规范拦住了一个具体错误"。八行记录里，**五行的原文用的是"继续"**：

- 差点**继续**用 `msg.startsWith("unknown station")` 判断 HTTP 状态码
- 差点**继续**把首页写成 `use client` + `useEffect` 拉数据
- 差点**继续**把 `@Entity` 标在领域对象上

**没有一条是"模型不知道"。** 每一条都是"模型知道，但它照着本地已有的坏代码写下去了"。

模型最危险的不是无知，是**恭顺**。

这个区分改变了整件事：如果拦的是无知，那规范的价值会随模型升级而自动归零；如果拦的是恭顺，那它**永远不会自动归零**，因为项目里永远有旧代码。

## 三、然后我们发现了更要命的

既然"读库有没有用"不好测，我们换了个角度：**看机制。**

三个仓里，只有一个被一个新鲜度脚本盯着。跑一次：

| 仓 | 被仪器盯着？ | 状态 |
| :--- | :--- | :--- |
| 工具链仓 | **是** | 新鲜 |
| 知识库 | 否 | **落后 21 个提交** |
| 项目仓 | 否 | **根本没登记** |

**100% 相关，不需要任何假设。**

结论不是"这知识库有用"，是一句更朴素的话：

> **机制只管它被安装的地方。**

但那还只是开始。真正的教训在后面 ——

## 四、我们装了两台坏仪器，而且是不同坏法

### 第一台：写好了，但没接线

一个新鲜度脚本，写完就放着。**两天没跑过。**

接进 git hook 的**当天**，它立刻抓出三个仓里两个已烂。

> **写了脚本 ≠ 装了机制。** 没接线的仪器等于没装。

### 第二台：接了线，但永远亮绿灯

这个更值得说，因为它是我自己犯的。

我们发现有一类文件"有入口没出口"：拦截候选记录了，但流程的下一步（复盘入库）**没有任何东西触发** —— 候选一直挂着。

于是我给候选池装了台仪器：**候选挂了超过 14 天没处理，就报警。**

装完跑了一遍。绿的。

我当时很满意，直到一天后核对：

> **候选池里 6 行，全是旧的。最近 111 个波次，新增候选 = 0。**

我假设的失效是"**候选堆积、没人处理**"。
实际的失效是"**候选根本不产生**"。

**所以那台仪器永远不会亮。它测的是一个不存在的病。**

> 没接线的仪器等于没装。
> 但还有更坏的一档：**接了线、却恒绿的仪器** ——
> 它比没有更危险，因为它**看起来像在监控**。

由此得出一条我自己现在还在用的判据：

> **给"停滞"装仪器时，要测「产出为 0」，不要测「积压 > 0」。**
>
> 积压只是停滞的一种形态。"什么都不产生"是另一种 ——
> 更常见，也更容易漏掉，**因为它看起来像在正常工作**。

## 五、111 个波次，0 条新知识

上面那个"111"不是修辞。

我们有一个 AI 集群在推进一个业务项目。有一天我离开，第二天回来看：

| 指标 | 离开时 | 回来时 |
| :--- | :--- | :--- |
| 波次 | 58 | **169** |
| 前端测试 | 35 | **185** |
| **新增知识条目** | — | **0** |

**111 个波次，测试翻了 5 倍，知识零增量。**

我去看那 111 个波次在做什么。答案是：**把已经确立的模式重复应用到下一个域**。

那段时间的提交名全是 `XxxView`、`YyyView` —— 把一个已经成熟的写法，一个域一个域地铺过去。

**机械应用撞不到墙。撞不到墙就没有新知识。**

所以：

> **换一条线继续跑，不等于换了压力。**
>
> 我们先是把一条线（后端持久化）挖到了矿脉尽头，然后换了一条线（前端视图模型），
> 又挖到了尽头 —— 但我们以为换了线就是换了矿。

而这期间，**政策里明明写着一条刹车**：

> "连续 5 个周期没有新知识 → 停止。"

111 个周期为零，没停。

**判据存在、可观察（数一下就知道）、但没有任何东西去数它。**

## 六、所以这套东西到底是什么

它不是"一套更好的 AI 提示词"，也不是"一个文档库"。

它是三个机制：

**1. 两个账本，一个记收益一个记缺口**

```
拦截账本    这条规范拦住了什么   → 决定留哪条
缺口账本    这条规范没能回答什么 → 决定补哪条
```

它们必须成对。**只有收益侧没有缺口侧，账本会变成功劳簿。**

**2. 条目可以"毕业"**

最值钱的规范不该永远躺在文档里。当它的内容被写进一个测试之后，那条规范就**退出索引** —— 它没有过时，它**完成了**。

一个会**收缩**的知识库，比一个"有 121 条"的知识库可靠得多。

**3. 每条新增都必须回答：不读它，会错在哪？**

说不出来就别进库。这条门槛筛掉了绝大部分"看起来很有道理"的话。

## 七、四条可以搬走的判据

前面讲了那么多失败，真正可搬走的是这四条 —— **不是 121 个条目**：

### ① 无机制的政策是负优化

加了规则却更慢，**不是"还没优化够"**，是负优化。

自检三问（任一是否 → 停写政策，先装机制）：
1. 违反能否在 60 秒内被**外部**观察到？
2. 发现后有**强制动作**吗（不是"记一笔"）？
3. 执行面具备吗？

### ② 仪器：测「产出为 0」，不要测「积压 > 0」

见第四节。这是我自己踩出来的。

### ③ 声称需要被强制，否则静默漂移

代码里有类注释，声称跨切面属性 —— "同事务双写"、"线程安全"、"已校验"。

我们实测：某用例的注释写着"**同事务双写**"，而**整个仓库没有一处 `@Transactional`**。

后果不是慢，是**数据错**：两次写各写各的，第二次失败时第一次已提交 ——
站里的电池被取走，日志没有记录。**而且用户那侧看起来一切正常。**

> 读到这类注释，先花 5 秒 `grep` 一句：**谁在强制它？**

### ④ 核实的范围，由你的想象决定

同一个动作，连续两次不同遗漏：

- 一次**没核实数据源是否存在** —— 下令"改成服务端渲染"，到动手才发现**后端根本没有读接口**
- 一次核实了生产实现，**却漏了三个测试替身** —— 编译当场失败

**两次都不是"没核实"，是"核实的范围不够"。**

> 动手前，列出这条改动会碰到的**所有实现者**。
> 列不全的理由从来不是懒，是**没想到有这一类**。

## 八、最后

回到开头那个"不体面"的实验。

它测出零差异，我们**没有把它藏起来**。它现在还在库里，是那个知识库关于自己最诚实的一条记录。

八天里我们还做过这些事：

- 删掉一个**假绿灯**的 CI —— 那个检查写了个 `echo TODO` 就当作通过
- 删掉一个**编造的数字** —— 某条分析里写着"边际价值增加 3-5 倍"，没有任何来源
- 把一条"AI 永远不持有写权限"的规则改掉 —— 因为它与事实相反（AI 确实有文件权限），而**一条无法执行的规则会稀释全部规则的权威**
- 写了一条模式，专门用来**指控自己最近三个版本的负优化**

**这套东西最像样的能力，是它否定自己的能力。**

它已经知道自己该砍哪些条目、该接哪根线、该停哪个集群了 ——
答案都写在它自己的条目里，只是还没执行回自己身上。

而这，恰好就是它教给我们的最后一课：

> **一个知识系统的价值，不在于它知道多少，
> 而在于它能不能在没有外部监督的情况下，抓住自己的错误。**

---

*本文所述系统由三部分组成：一个 markdown + YAML 的知识库、一个驱动它的命令行工具链、
以及一个用来压测它的真实业务项目。全部证据（含失败的实验）都在仓库里。*
