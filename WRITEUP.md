# 一份会否定自己的知识库

> 关于 `COLLABORATION` 的技术说明 —— 写给外部读者。
> 不是使用指南（那看 `integrations/usage-guide.md`），是**它是什么、凭什么、以及它在哪失效**。

## 一、问题：AI 让生产变便宜之后

当写代码、写文档、写条目的边际成本趋近于零，**瓶颈从"生产"迁移到了"选择"**：

- 生成一个条目很便宜 → 于是条目会无限膨胀
- 生成一个 agent 很便宜 → 于是并行度会无限膨胀
- 两者都会让**信噪比下降**，直到整个系统不再有用

这个仓库是对该问题的一个回答，也是对它的一次**八天连续实验**。

## 二、它是什么

一个**带生命周期和结算机制**的 markdown + YAML 知识库，配一个 CLI/MCP 工具链：

```
collaboration/          长期知识（要活着，就得能被删）
  agreements/  workflows/  skills/  patterns/  integrations/
  meta/                演化机制本身：账本 / ADR / 修剪政策 / 基座契约
collab-cli/             工具：validate / catalog / retire / memory / mcp
<业务仓>/working-memory/  进度（高频改、允许错、不进 KB）
```

三条设计主张：

1. **价值 KPI 是"拦住了什么"，不是条目数。**
   `meta/interceptions.md` 每行记"某条目拦住了一个具体错误 + 证据"。
   长期零拦截的条目 → 修剪候选。

2. **修剪是主功能，不是维护负担。**
   条目生命周期 `draft → active → dormant`，`dormant` **退出路由索引**
   （文件保留，git 守基因）。删除的成本必须低，否则没人敢删。

3. **规范必须能变成机制，否则它不存在。**
   最值钱的条目会"毕业"：内容被写进测试后，条目**退出索引**
   （`enforced` 字段指向那个测试）。**一个会收缩的知识库。**

## 三、凭什么说它有用：三个真实实验

### 实验 1：n=3 的自然实验（最强的一个）

三个仓里，只有一个被新鲜度脚本盯着。结果：

| 仓 | 被仪器盯？ | 状态 |
| :--- | :--- | :--- |
| `collab-cli` | **是** | ✅ 新鲜 |
| `collaboration` | 否 | ❌ 落后 21 提交 |
| 项目仓 | 否 | ❌ 未登记 |

**100% 相关，无需假设。** 结论不是"这知识库有用"，是
**"机制只管它被安装的地方"** —— 而这后来成了整个项目最重要的一条经验。

### 实验 2：唯一干净的对照实验，测出零差异

在空项目里跑双臂（有库 / 无库）：**代码结构无差异**，读库的臂
**墙钟 +55%**。结论当时写得很克制：「模型自己就会把不变量放对地方」。

**这条没有被藏起来，它写进了库里。**

但它有边界 —— 它测的是"模型**知不知道**正确答案"，而不是
"模型会不会**被本地代码带偏**"。后者的证据在别处：账本 8 行里
**5 行的原文用的是"继续"**（差点**继续**用 `msg.startsWith` 判状态码 /
差点**继续**全 `use client`）。**真正拦住的是恭顺，不是无知。**

### 实验 3：失败也是数据

111 个并行波次之后回看：**L2 涨了 111 波、前端测试 ×5.3，而 L3 增量 = 0**。

这直接暴露了两件事（都写进了库）：

- **换一条线继续跑，不等于换了压力。** "View 化"是既有模式的机械应用，
  **撞不到墙**，所以产不出任何新知识。
- **政策里那条刹车从没接上。** 政策写着"连续 5 tick 无增量 → 停"，
  实际 111 波无增量、没停。

## 四、四个可以搬走的东西

不是 121 条正文 —— 是这四条**判据**：

### ① 无机制的政策是负优化

加了规则却更慢，**不是"还没优化够"**。判定三问（任一「否」→ 停写政策）：

1. 违反能否 ≤60s 内被外部观察到？
2. 发现后有**强制动作**吗（不是"记一笔"）？
3. 执行面具备吗？

### ② 仪器：测"产出为 0"，不要测"积压 > 0"

我们先后装了 5 台仪器。**接线的都立刻起作用**：
新鲜度脚本接进 pre-push **当天**就抓出 2/3 仓已烂。

但也踩了两个坑，都是我自己踩的：

| 坑 | 实例 |
| :--- | :--- |
| **没接线的仪器等于没装** | 脚本写好但空转两天 |
| **接了线却是"绿线"** | 给候选池装了龄期检查，假设"候选堆积没人处理" —— 实际**候选根本不产生**，所以它**永远恒绿** |

第二类比没有仪器更危险：**它看起来像在监控。**

### ③ 声称需要被强制，否则静默漂移

代码里有一类注释，声称跨切面属性（"同事务双写" / "线程安全"）。
实测：某用例注释写"同事务双写"，而**全仓零 `@Transactional`** ——
两次写各写各的，第二次失败时第一次已提交。**数据错且静默。**

⇒ 读到这种声称，先一条 `grep` 问"谁在强制它"。

### ④ 核实的范围，由你的想象决定

同一个动作连续两次不同遗漏：一次没核实**数据源是否存在**（下令后才发现
没有读端点），一次核实了生产实现**却漏了三个测试替身**。

**不是"没核实"，是"核实的范围不够"。**
⇒ 动手前，列出这条改动会碰到的**所有实现者**。

## 五、诚实的现状

| 项 | 状态 |
| :--- | :--- |
| 条目 | 121 条（**多不多不是重点**） |
| 真实拦截记录 | 14 行 |
| **"读了有没有用"** | **至今不可判定** —— 唯一干净实验测出零差异，账本全是自述 |
| 被证据支持的 | **机制**（测试 / 工具 / CI / 仪器），不是条目内容 |

**这台东西最像样的能力，是它否定自己的能力。**
它删过自己的假绿灯、改过与事实相反的规则、写过一条 pattern 来指控自己。
**方法比内容值钱。**

## 六、如果你只想搬一样东西

搬**两个账本**：

```
interceptions.md   条目拦住了什么   → 决定留哪条
known-gaps.md      条目没能回答什么 → 决定补哪条
```

它们是一对。**只有收益侧没有缺口侧，账本会变成功劳簿。**

以及那条入库门槛：

> **新增条目必须能说出它拦住了什么。说不出来，就别进库。**
